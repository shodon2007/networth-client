import cls from 'Messages.module.scss';

interface MessagesProps { text: string; date: Date; edited: boolean;  }

export const Messages = ({}: MessagesProps) => {

  

  return (
    <div></div>
  )
}